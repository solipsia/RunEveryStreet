function setup() {
	//createCanvas(windowWidth, windowHeight);
	let canvas = createCanvas(600, 400);
	canvas.position(0, 0);
	colorMode(HSB);
}

function draw() {
	//background(0);
	rect(10, 10, 10, 10);
}